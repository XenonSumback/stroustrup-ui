import React from 'react';
import CreateAccountScreen from '../../components/Registration/';

export default class RegistrationPage extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function

  render() {
    return (
      <CreateAccountScreen />
    );
  }
}
